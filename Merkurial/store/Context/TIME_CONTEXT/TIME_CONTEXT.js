// import { createContext, useState } from "react";

// let time = 6000;

// export const TIME_CONTEXT = createContext({
//   blogUpdateTIme: 6000,
//   startTimer: () => {},
// });

// REMOVE_FROM_LOCAL_STORAGE("hasLoaded");
// SAVE_TO_LOCAL_STORAGE({ hasLoaded: false }, "hasLoaded");

// const TIME_CONTEXT_PROVIDER = (props) => {
//   timeContextValue = {
//     time: time,
//   };

//   return <TIME_CONTEXT.Provider>{props.children}</TIME_CONTEXT.Provider>;
// };

// export default TIME_CONTEXT_PROVIDER;
